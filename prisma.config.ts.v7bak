import 'dotenv/config'
import { defineConfig } from 'prisma/config'

console.log('CONFIG LOADED, DATABASE_URL =', process.env.DATABASE_URL)

export default defineConfig({
  migrations: {
    seed: 'tsx ./prisma/seed.ts',
  },
  datasource: {
    url: process.env.DATABASE_URL,
  },
})
